plugins {
    id 'com.gradle.build-scan' version '2.3'
}

repositories {
    jcenter()
}

allprojects {
    apply plugin: "java"

    repositories {
        jcenter()
    }
}

subprojects {
    version = '1.0'
}